import { VflAnchorDirective } from './vfl-anchor.directive';

describe('VflAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new VflAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
